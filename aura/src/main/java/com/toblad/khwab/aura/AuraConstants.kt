package com.toblad.khwab.aura

/**
 * Constants used throughout the Khwab Aura module.
 */
object AuraConstants {

    /**
     * Default interval for refreshing Aura.
     */
    const val DEFAULT_REFRESH_INTERVAL_MINUTES = 60

    /**
     * Default animation duration in milliseconds.
     */
    const val DEFAULT_ANIMATION_DURATION_MS = 1000L

    /**
     * Default cloud animation speed multiplier.
     */
    const val DEFAULT_CLOUD_SPEED = 1.0f

    /**
     * Default rain intensity multiplier.
     */
    const val DEFAULT_RAIN_INTENSITY = 1.0f

    /**
     * Default snow intensity multiplier.
     */
    const val DEFAULT_SNOW_INTENSITY = 1.0f

    /**
     * Default lightning flash duration.
     */
    const val DEFAULT_LIGHTNING_DURATION_MS = 300L
}

